import 'package:flutter/material.dart';

class ChatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Working to provide you this feature.',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
